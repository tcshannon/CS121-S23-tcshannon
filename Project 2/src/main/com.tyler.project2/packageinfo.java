/**
 * This package contains classes for a simple Pokemon game.
 * The game allows two players to select their Pokemon and battle them against each other.
 *
 * The package contains the following classes:
 * - Pokemon: defines a Pokemon and its properties.
 * - PokemonSelection: handles the selection of Pokemon for each player.
 * - Main: contains the main method to run the game.
 *
 * Example usage:
 * PokemonSelection pokemonSelection = new PokemonSelection();
 * pokemonSelection.assignPokemon(1);
 * pokemonSelection.assignPokemon(2);
 */
package main.com.tyler.project2;
